@model Model.SelectSub

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>SelectSub</legend>

        @Html.HiddenFor(model => model.SelectSubID)

        <div class="editor-label">
            @Html.LabelFor(model => model.StuID, "Student")
        </div>
        <div class="editor-field">
            @Html.DropDownList("StuID", String.Empty)
            @Html.ValidationMessageFor(model => model.StuID)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.SubID, "Sub")
        </div>
        <div class="editor-field">
            @Html.DropDownList("SubID", String.Empty)
            @Html.ValidationMessageFor(model => model.SubID)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
